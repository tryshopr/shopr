- @page_title = t(:delivery_services)
= content_for :header do
  %p.buttons
    = link_to t(:prices), [@delivery_service, :delivery_service_prices], :class => 'button' 
    = link_to t(:delivery_services_back_to), :delivery_services, :class => 'button'
  %h2.delivery_services= t(:delivery_services)
= render 'form'