.addresses
  .table
    %table.data
      %thead
        %tr
          %th= t('shopr.customers.type')
          %th= t('shopr.customers.default')
          %th= t('shopr.custoemrs.address')
          %th
      %tbody
        - if @addresses.empty?
          %tr.empty
            %td{colspan: 4}= t('shopr.customers.no_addresses')
        - else
          - for address in @addresses
            %tr
              %td= address.address_type.capitalize
              %td= boolean_tag address.default
              %td= address.full_address
              %td= link_to t('shopr.customers.edit'), edit_customer_address_path(@customer, address)
