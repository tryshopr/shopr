- @page_title = t("shopr.settings.settings")

= content_for :header do
  %h2.settings= t("shopr.settings.settings")

= form_tag :url => settings_path do |f|

  - Shopr.settings_groups.each do |group, fields|
    = field_set_tag t("shopr.settings.#{group}") do
      - fields.each do |f|
        %dl.sided
          %dt= settings_label f
          %dd
            = settings_field f
            - if v = t("shopr.settings.help")[f]
              %p.help= v

  %p.submit.sided
    = submit_tag t("shopr.settings.save_settings"), :class => 'button blue'
